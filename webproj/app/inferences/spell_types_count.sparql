PREFIX : <http://hogwarts.edu/ontology.owl#>
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

SELECT ?type (COUNT(?spell) AS ?count)
WHERE {
  ?spell rdf:type :Spell .
  ?spell :hasType ?type .
}
GROUP BY ?type
